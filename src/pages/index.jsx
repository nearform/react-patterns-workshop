// !! Start here !!

// Uncomment to import useQuery
// import { useQuery } from 'react-query'

const MovieExplorerApp = () => {
  // Your query here
  // Make sure to call `.json()` on the fetch results

  // Make sure data is available by checking the `data` property

  /**
   * When the data is available it will be in the format:
   * ```
   * {
   *   results: [{id: 'an-id', title: 'A movie title', image: 'path-to-an-image' }]
   * }
   * ```
   */

  return <div>TODO map over the results here</div>
}

export default MovieExplorerApp
